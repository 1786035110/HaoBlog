# 阶段四准入清单：评论、工具箱、搜索与综合验收

## S4-10 本地收口状态

2026-08-23 本地综合验收完成。环境为 Java 21.0.11、Node.js 24.14.0、pnpm 11.16.0、Docker Engine 29.7.2、Compose 5.3.1。下面的勾选只表示本地可复现结果，不代表远端或真实生产已验收。

### 契约、代码与数据库

- [x] Java 21、Node.js 24、pnpm 11.16.0、Docker Engine 29.7.2、Compose 5.3.1 已确认。
- [x] API `-DskipITs verify` 通过，Surefire 78/78。
- [x] PostgreSQL/pgvector Testcontainers Failsafe 通过，53/53。
- [x] Web typecheck、Vitest 85/85 和 production build 通过。
- [x] OpenAPI generate、check 和生成文件无差异通过。
- [x] dev/prod Compose config 与资源/健康边界校验通过。

### 运行态与 E2E

- [x] 独立项目 `haoblog-s4-10-acceptance` 使用测试镜像启动，Caddy、Web、API、PostgreSQL/pgvector 均 healthy。
- [x] Smoke 通过，Actuator 未从 Caddy 公网路径暴露。
- [x] 完整 Chromium Playwright 14/14 通过；覆盖四档视口、键盘、无 JavaScript、Save-Data、reduced-motion、SSR、评论、Studio、工具和搜索。
- [x] 游客获取 CSRF/挑战、待审提交、挑战重放、恶意输入、限频、删除令牌、公共评论和全局/单篇开关通过。
- [x] Studio 审核待审、已通过、垃圾、拒绝、一级回复和用户删除状态通过。
- [x] Studio 工具 CRUD、LINK、SHOWCASE、五个 EMBEDDED、收藏/最常用排序、Worker 输入输出与网络隔离通过。
- [x] 中文标题/摘要/正文搜索、特殊字符、分页、无 JavaScript 和公开边界通过。

### 性能、资源与安全

- [x] 文章初始客户端静态闭包 65,244 bytes gzip，小于 180 KiB；工具组件和 Worker 不进入文章初始包。
- [x] 文章页和工具页 Lighthouse 三次运行的 Performance、Accessibility、SEO 均为 1.00，均不低于 0.90。
- [x] Caddy/Web/API/PostgreSQL 的内存、CPU、PID 上限保持 64/320/640/480 MiB、0.2/0.6/1.25/0.8 CPU、64/128/256/128 PID。
- [x] 收口观察值为 API 409.4 MiB、Caddy 16.79 MiB、PostgreSQL 47.93 MiB、Web 105.7 MiB。
- [x] 日志未发现评论正文、邮箱、IP、删除令牌、安全密钥或 SMTP 密码。
- [x] SMTP 故障隔离、大文本限制和正则 Worker 超时有单测/集成覆盖；正则 Worker 超时为 250ms。
- [x] `git diff --check` 通过。

## 阶段四验收命令

依赖、代码、契约和配置：

```powershell
Push-Location apps/api
.\mvnw.cmd -DskipITs verify
.\mvnw.cmd failsafe:integration-test failsafe:verify
Pop-Location
corepack pnpm --dir apps/web typecheck
corepack pnpm --dir apps/web test
corepack pnpm --dir apps/web build
corepack pnpm --filter @haoblog/api-client generate
corepack pnpm --filter @haoblog/api-client check
git diff --exit-code -- packages/api-client/src/generated.ts
docker compose --env-file .env.example -f infra/compose/compose.dev.yml config
docker compose --env-file infra/compose/.env.ci.example -f infra/compose/compose.prod.yml config
corepack pnpm compose:verify
```

生产编排、Smoke、E2E、性能与清理：

```powershell
docker build -f apps/api/Dockerfile -t haoblog-api:s4-10-final .
docker build -f apps/web/Dockerfile -t haoblog-web:s4-10-final .
$env:HAOBLOG_API_IMAGE = 'haoblog-api:s4-10-final'
$env:HAOBLOG_WEB_IMAGE = 'haoblog-web:s4-10-final'
$env:HAOBLOG_PUBLIC_BASE_URL = 'http://localhost'
docker compose -p haoblog-s4-10-acceptance --env-file infra/compose/.env.ci.example -f infra/compose/compose.prod.yml up -d --wait --wait-timeout 180
$env:HAOBLOG_BASE_URL = 'http://localhost'
corepack pnpm smoke
$env:CI = 'true'
corepack pnpm --dir apps/web e2e
corepack pnpm web:budget
$env:LHCI_EXISTING_SERVER = 'true'
$env:LHCI_BASE_URL = 'http://localhost'
$env:LHCI_ARTICLE_PATH = '/articles/s3-08-advanced-markdown'
corepack pnpm web:lighthouse
$env:LHCI_ARTICLE_PATH = '/tools'
corepack pnpm web:lighthouse
git diff --check
docker compose -p haoblog-s4-10-acceptance --env-file infra/compose/.env.ci.example -f infra/compose/compose.prod.yml down -v --remove-orphans
```

## 未执行项目

真实 SMTP 投递、真实域名与 HTTPS、真实 OSS、镜像推送、部署、远端 CI 和远端生产验收均未执行。`infra/compose/.env.ci.example` 的账号、密钥和 OSS 值只用于临时本地验收，不能用于生产。
