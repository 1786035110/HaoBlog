server:
  forward-headers-strategy: framework
  servlet:
    session:
      cookie:
        secure: true

haoblog:
  site:
    public-base-url: ${HAOBLOG_PUBLIC_BASE_URL}
    music-manifest-url: ${HAOBLOG_MUSIC_MANIFEST_URL:}
  security:
    session-cookie-secure: true
